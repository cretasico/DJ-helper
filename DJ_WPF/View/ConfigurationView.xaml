<UserControl x:Class="DJ_WPF.View.ConfigurationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#1E1E1E">
    <Grid Margin="10" Background="#1E1E1E">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="Configuration Settings" 
                   FontSize="18" 
                   FontWeight="Bold" 
                   Margin="0,0,0,10" 
                   Foreground="White" />

        <!-- Configuration Fields -->
        <StackPanel Grid.Row="1" Margin="0,10,0,10">
            <TextBlock Text="Excel Path:" Foreground="White" />
            <TextBox Text="{Binding ExcelPath}" Margin="0,5,0,10" Background="#333333" Foreground="White" />

            <TextBlock Text="Old Excel Path:" Foreground="White" />
            <TextBox Text="{Binding ExcelPathOld}" Margin="0,5,0,10" Background="#333333" Foreground="White" />

            <TextBlock Text="Source Path:" Foreground="White" />
            <TextBox Text="{Binding SourcePath}" Margin="0,5,0,10" Background="#333333" Foreground="White" />

            <TextBlock Text="Spotify API Key:" Foreground="White" />
            <TextBox Text="{Binding SpotifyAPIKey}" Margin="0,5,0,10" Background="#333333" Foreground="White" />

            

            <TextBlock Text="Rating Scale - Normal:" Foreground="White" />
            <TextBox Text="{Binding LevelNormal}" Margin="0,5,0,10" Background="#333333" Foreground="White" />

            <TextBlock Text="Rating Scale - Good:" Foreground="White" />
            <TextBox Text="{Binding LevelGood}" Margin="0,5,0,10" Background="#333333" Foreground="White" />

            <TextBlock Text="Rating Scale - Super:" Foreground="White" />
            <TextBox Text="{Binding LevelSuper, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10" Background="#333333" Foreground="White" />

        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Modify" 
                    Command="{Binding SaveConfigurationCommand}" 
                    Margin="0,0,10,0" 
                    Background="#333333" 
                    Foreground="White" />
            <Button Content="Cancel" 
                    Command="{Binding CancelConfigurationCommand}" 
                    Background="#333333" 
                    Foreground="White" />
        </StackPanel>
    </Grid>
</UserControl>